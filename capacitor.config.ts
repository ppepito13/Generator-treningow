import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pepitolabs.generator',
  appName: 'Generator Treningów SW',
  webDir: 'out'
};

export default config;
